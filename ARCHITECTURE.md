# Architecture

The architecture document lives in the wiki — see [`wiki/Architecture.md`](wiki/Architecture.md).

It covers: package map, architectural boundaries, exception hierarchy,
async concurrency model, YouTube extractor design, configuration, persistence,
pipeline event model, logging, generation logic, CLI surface, and naming conventions.
